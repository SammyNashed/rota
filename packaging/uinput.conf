# uinput is a module on Arch, not builtin. /dev/uinput exists as a static node
# even when it is not loaded, so opening it fails with ENODEV rather than
# ENOENT — which looks like a permissions problem and is not one.
uinput
